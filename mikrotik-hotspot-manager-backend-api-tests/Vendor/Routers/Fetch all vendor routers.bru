meta {
  name: Fetch all vendor routers
  type: http
  seq: 1
}

get {
  url: {{local_url}}/vendor/routers
  body: none
  auth: bearer
}

params:query {
  ~page: 1
}

auth:bearer {
  token: {{auth_token}}
}
